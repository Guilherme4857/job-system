<h1>Vagas Abertas</h1> 
<h2>
<%flash.each do |msg|%>
  <div><%=msg%></div>
<%end%>
<%if @jobs%>
<div> Sem anúncio de vaga <div>
<%end%>

</h2>


<%@jobs.each do |job| %>
  <P>
  <h3><%=link_to job.title, job_path(job)%> (ver detalhes)</h3> 
  <div><%=job.job_attributes(3)%>: <%= job.level%></div>
  <div><%=job.job_attributes(4)%>: <%=job.requirements%></div>
  <div><%=job.job_attributes(5)%>: <%= localize job.expiration_date%></div>
  </P>
  <hr>
<% end %>

<div><%=link_to 'Anuncie sua vaga', new_job_path%></div>

<p>
<div><%=link_to 'Voltar', root_path%></div>
</p>